New goals for jguardrail:
    - missing braces for if and while
      if (a < b)
          System.out.println("something")

    - Disallow incorrect indentation

    - Maybe also missing "this." before instance vars???

